function findMinAndRemoveSorted(array) {
  let min = array[0];
  array.shift();
  return min;
}

function merge (a1, a2) {
  sorted = []
  while (a1.length !== 0 && a2.length !==0) {
    if (a1[0] < a2[0]) {
    
    }
  }
}